# Yrgo Tivoli - Whack a Mole

# Packages

Axoios: npm install axios
API for better handling of HTTPS requests.

Swagger UI: npm install swagger-ui-dist
Used for testing API endpoints.

Also uses serve@14.2.4.
Type npx serve public to run swagger.html
